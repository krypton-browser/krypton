/*
import electron from 'electron';
import axios from 'axios';
import fs from 'fs'

export const userDataPath = (electron.app || electron.remote.app).getPath('userData');

default export async function(){
  await axios('');
  const fileStream = fs.createWriteStream(path);
}
*/

/*
import data from './data.data.json';

export default function check(url: string) {
  return (data as unknown as Array<any>).some((x) => {
    try {
      return new URL(x?.url).host === new URL(url).host;
    } catch {
      return x?.url === url;
    }
  });
}
*/
